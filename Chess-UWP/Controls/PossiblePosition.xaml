<UserControl
    x:Class="Chess_UWP.Controls.PossiblePosition"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Chess_UWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:Chess_UWP.Infrastructure.Converters"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="100">

    <UserControl.Resources>
        <converters:PositionToMarginConverter x:Key="PositionToMarginConverter"/>
    </UserControl.Resources>

    <ContentControl Padding="25">
        <Ellipse Opacity="0.6"
                 Width="50" Height="50"
                 Fill="DarkGray"
                 IsHitTestVisible="False"
                 Margin="{Binding Position, Converter={StaticResource PositionToMarginConverter}}"/>
    </ContentControl>
</UserControl>
